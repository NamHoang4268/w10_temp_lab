apiVersion: templates.gatekeeper.sh/v1
kind: ConstraintTemplate
metadata:
    name: k8sdisallowlatesttag
    annotations:
        argocd.argoproj.io/sync-wave: "0"
spec:
    crd:
        spec:
            names:
                kind: K8sDisallowLatestTag
    targets:
        - target: admission.k8s.gatekeeper.sh
          rego: |
              package k8sdisallowlatesttag

              violation[{"msg": msg}] {
                container := input_containers[_]
                endswith(lower(container.image), ":latest")
                msg := sprintf("container %q uses forbidden image tag :latest", [container.name])
              }

              input_containers[c] {
                spec := pod_spec
                c := spec.containers[_]
              }

              input_containers[c] {
                spec := pod_spec
                c := spec.initContainers[_]
              }

              pod_spec := input.review.object.spec {
                input.review.kind.kind == "Pod"
              }

              pod_spec := input.review.object.spec.template.spec {
                template_kinds[input.review.kind.kind]
              }

              pod_spec := input.review.object.spec.jobTemplate.spec.template.spec {
                input.review.kind.kind == "CronJob"
              }

              template_kinds := {
                "DaemonSet",
                "Deployment",
                "Job",
                "ReplicaSet",
                "Rollout",
                "StatefulSet",
              }
